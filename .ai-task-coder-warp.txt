Simulated Warp execution for issue #1847
Task: Modify a central location (e.g., app/controllers/application_controller.rb or a dedicated service object) to read and aggregate the build provenance data (Git SHA, branch, and build time) from environment variables or build artifacts. This ensures the data is available without platform credentials (AC3).
Next, implement a dedicated endpoint, such as /version, and/or modify the application middleware to expose this aggregated provenance data in a response header (AC4).
Agent: coder
