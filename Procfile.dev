web: bin/rails server
# Single compiler for dev + CI: the tailwindcss-ruby binary bundled by
# tailwindcss-rails. `-w always` keeps the watcher alive when foreman closes
# stdin (upstream --watch exits on stdin EOF, which killed bin/dev, #3017).
css: bin/rails "tailwindcss:watch[always]"
