# EXAMPLE USAGE
# Refer for explanation to following link:
# https://evilmartians.github.io/lefthook/configuration/no_tty.html
# SOME COMMANDS RELY ON THE MISE SHIMS BEING ON PATH

shell:
  pty: false
verbose: false
pre-push:
  pre-command: git add . && git stash
  post-command: |
    if [[ -n $(git ls-files --others --exclude-standard) ]]; then
      echo "❌ Error: New files were generated during the pre-push hook:"
      git ls-files --others --exclude-standard
      echo "Please commit these files before pushing."
      exit 1
    fi
  commands:
    rails-tests:
      tags: backend tests
      run: |
        echo "🚀 Setting up test environment..."
        if ! CI=true RAILS_ENV=test bin/setup --skip-server; then
          echo "❌ Failed to set up test environment"
          exit 1
        fi
        
        echo "🔧 Preparing test database..."
        if ! CI=true RAILS_ENV=test bin/rails test:prepare --trace; then
          echo "❌ Failed to prepare test database"
          exit 1
        fi
        
        # Set up environment explicitly
        export PATH="/Users/temp/.local/bin:$PATH"
        export MISE_SHELL=zsh
        
        # Debug output
        echo "=== Environment ==="
        echo "PATH: $PATH"
        echo "Node: $(node --version)"
        echo "Ruby: $(ruby --version)"
        
        # Run tests with simplified environment
        if ! RAILS_ENV=test bin/rails test; then
          echo "❌ Tests failed"
          exit 1
        fi
        
        echo "✅ All tests passed"
    test-next:
      tags: backend
      run: |
        echo "🚀 Setting up next test environment..."
        if ! CI=true RAILS_ENV=test bin/setup-next; then
          echo "❌ Failed to set up next test environment"
          exit 1
        fi
        
        echo "🧪 Running next tests..."
        if ! CI=true RAILS_ENV=test HEADLESS=true CUPRITE=true APP_HOST='127.0.0.1' next rails test; then
          echo "❌ Next tests failed"
          exit 1
        fi
        
        echo "✅ All next tests passed"
    gems-audit:
      tags: backend security
      run: bundle audit update && bundle audit check
    javascript-audit:
      tags: frontend security
      run: mise exec -- yarn npm audit --all
    importmaps-audit:
      tags: frontend security
      run: bin/importmap audit
    rubocop:
      tags: backend style
      files: git diff --name-only develop
      glob: '*.rb'
      run: bundle exec rubocop --force-exclusion -A {files}
    brakeman:
      tags: backend security
      run: bundle exec brakeman --no-pager
    rails-system-tests:
      tags: backend tests
      run: |
        echo "🚀 Setting up system test environment..."
        if ! CI=true RAILS_ENV=test bin/setup --skip-server; then
          echo "❌ Failed to set up system test environment"
          exit 1
        fi
        
        echo "🔧 Preparing test database..."
        if ! CI=true RAILS_ENV=test bin/rails test:prepare --trace; then
          echo "❌ Failed to prepare test database"
          exit 1
        fi
        
        echo "🧪 Running system tests..."
        if ! CI=true RAILS_ENV=test HEADLESS=true CUPRITE=true APP_HOST='127.0.0.1' CUPRITE_JS_ERRORS=true bin/rails test:system; then
          echo "❌ System tests failed"
          exit 1
        fi
        
        echo "✅ All system tests passed"
    verify_ci_ready:
      run: bin/container-healthcheck.sh && echo "✓ CI-ready environment" || { echo "✗ Environment mismatch detected"; exit 1; }
      tags: ci

pre-commit:
  fail_fast: true
  pre-command: git stash --keep-index
  parallel: false
  commands:
    validate-actions:
      description: 'Validate GitHub Actions workflow files'
      glob: '.github/workflows/*.yml'
      run: |
        echo "Validating GitHub Actions workflows..."
        for file in {staged_files}; do
          echo "• Validating $file"
          # Use act directly with the correct image configuration
          act --dryrun -W "$file" -P ubuntu-latest=node:20 || exit 1
        done
        echo "✓ All workflow files are valid"
    js:
      glob: '*.{js,ts}'
      run: yarn prettier --write {staged_files}
      stage_fixed: true
    rubocop:
      tags: backend style
      glob: '*.rb'
      exclude: 'application.rb|routes.rb'
      run: bundle exec rubocop -a --force-exclusion {all_files}
      stage_fixed: true
    erb-lint:
      tags: frontend style
      files: git diff --name-only develop
      glob: '*.erb'
      run: bundle exec erb_lint --lint-all {staged_files}
    yaml-lint:
      tags: frontend style
      files: git diff --name-only develop
      glob: '*.yml'
      run: bundle exec yaml-lint {staged_files}
      stage_fixed: true
    # rails-tests are run in pre-push instead of pre-commit for better performance
    # and to avoid breaking the commit flow with potentially long-running tests
    verify_environment:
      run: bin/container-healthcheck.sh && { echo "✓ Environment valid"; exit 0; } || { echo "✗ Environment invalid - run bin/setup-environment.sh"; exit 1; }
      tags: validation
      skip:
        - merge
        - rebase

fixer:
  verbose: false
  pty: false
  parallel: true
  commands:
    ruby-fixer:
      glob: '**/*.rb'
      run: bundle exec rubocop --autocorrect-all  --force-exclusion {all_files}
      stage_fixed: true
    js-fixer:
      glob: 'app/javascript/**/*.{js,ts,jsx,tsx}'
      run: yarn prettier --write {all_files}
      stage_fixed: true
    ruby-prettier:
      glob: 'app/**/*.rb'
      run: bundle exec rbprettier --write {files}
      stage_fixed: true
    erb_lint:
      glob: 'app/views/**/*.erb'
      run: bundle exec erb_lint --autocorrect {all_files}
      stage_fixed: true
    yaml-lint:
      glob: '**/*.yml'
      run: bundle exec yaml-lint {all_files}
      stage_fixed: true
    brakeman:
      run: bundle exec brakeman --no-pager
    yarn-audit:
      tags: frontend security
      run: yarn npm audit
    gems-audit:
      tags: backend security
      run: bundle audit update && bundle audit check
    i18n-tasks:
      run: bundle exec i18n-tasks health || bundle exec i18n-tasks normalize
      stage_fixed: true
    yarn-lint:
      tags: frontend updates
      glob: '{app/javascript/**/*.{js,ts,jsx,tsx},**/*.{css,scss,sass,md,json}}'
      run: yarn prettier --write {all_files}
      stage_fixed: true
    importmaps-audit:
      tags: frontend security
      run: bin/importmap outdated
# scripts:
#     "hello.js":
#       runner: node
#     "any.go":
#       runner: go run
#     "any.py":
#       runner: python
#     "any.rb":
#       runner: ruby
#     "any.sh":
#       runner: bash
