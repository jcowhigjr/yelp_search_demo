---
name: groom-backlog
description: Use when the user wants to refine, prioritize, or plan the backlog rather than implement it - triggered by `/groom-backlog`, "groom the backlog", "let's do backlog refinement", "what should we work on next", or "help me break this issue down". Interviews the user issue by issue, then records decisions in docs/ROADMAP.md.
---

# Groom Backlog

This skill is shared across every agent harness used in this repository. The
canonical definition lives in one place so it cannot drift:

**Read `.agents/skills/groom-backlog/SKILL.md` now and follow it exactly.**

Use the *Claude Code* column of its "Tooling adapter" table: `AskUserQuestion`
for interview questions, the `Agent` tool for delegated research, `Bash` for
`gh` and `git`.

Do not edit this file to change behaviour — edit the canonical file instead.
