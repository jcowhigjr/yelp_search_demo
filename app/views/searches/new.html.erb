<body>
    <div class="container">
        <% if flash[:error] %>
            <div class="row center">
                <span class="page-text red-text"><%= flash[:error] %></span>
            </div>
        <% end %>
          <h2 class="mt-2 text-3xl font-extrabold leading-8 tracking-tight text-gray-900 sm:text-4xl"><%= t('views.searches.new') %></h2>

         <%= render 'searches/form', locals: { search: @search } if @search %>
    </div>
</body>
